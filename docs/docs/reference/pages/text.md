# Text pages definition

{{ decorated_definition("Text page") }}

Text page contains abitrary content in 
Markdown. 

Possible usecases for it are: 

- welcome / entry info
- help info / guidance
- etc.

You can make a text page a welcoming one
(with an `[Enter contest]` button) by
toggling `is_enter_page` flag.

{{ decorated_definition("Entry page") }}

