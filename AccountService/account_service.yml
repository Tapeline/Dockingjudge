mode: local

rabbitmq:
  host: localhost
  username: rm_user
  password: rm_password

postgres:
  host: localhost
  port: 5500
  username: pguser
  password: pgpass

security:
  allowed_hosts: ["*"]

app:
  allow_registration: true
