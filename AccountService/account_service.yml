mode: local

rabbitmq:
  host: localhost
  username: rm_user
  password: rm_password

postgres:
  host: localhost
  port: 5500
  username: pguser
  password: pgpass

security:
  allowed_hosts:
    - "*"
    - "account.service:8000"
    - "account.service"
    - "dockingjudge.tapeline.dev"
    - "localhost"
    - "127.0.0.1"

app:
  allow_registration: true
